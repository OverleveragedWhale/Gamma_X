[margins]
# CME initial (performance bond) margin per contract, in USD.
# Verify current values at https://www.cmegroup.com/markets/equities.html
# (or your broker's margin page) and fill these in - they change often.
# Until filled in, the dashboard will show a clear per-symbol margin
# error instead of silently computing a wrong coverage % number.
ES = 25010
NQ = 39650
